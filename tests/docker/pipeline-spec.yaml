test:
  pipeline:
  - run: test
  - run: dump.to_path
    parameters:
      out-path: data

test-package:
  pipeline:
  - run: test
    parameters:
      test-package: true
  - run: dump.to_path
    parameters:
      out-path: data/test_package
